# Directory dinara /staticLibrary

This directory builds the Dinara static library `dinara.a`.
This library is used in two ways:

* It is linked in by the Dinara static executable `dinara`.
* It can be statically linked in by other C++ code that uses Dinara as a library.
