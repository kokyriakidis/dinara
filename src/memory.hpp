#ifndef DINARA_MEMORY_HPP
#define DINARA_MEMORY_HPP

#include <memory>

namespace dinara {
    using std::make_shared;
    using std::shared_ptr;
    using std::make_unique;
    using std::unique_ptr;
}

#endif
